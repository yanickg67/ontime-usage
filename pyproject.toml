[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "ontime-usage"
version = "1.0.2"
description = "A small shared counter for outbound API calls, per credential."
requires-python = ">=3.10"
dependencies = []

[tool.setuptools]
packages = ["ontime_usage"]
